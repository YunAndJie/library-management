# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/libaray_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 105627
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5

# 开发环境日志
logging:
  level:
    com.pwenjie: debug
    org.springframework: info

server:
  port: 8080
